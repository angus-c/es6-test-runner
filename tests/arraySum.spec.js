/*
 * (REPLACE THIS WITH YOUR TESTS)
 */

import arraySum from '../src/index.js';

describe('arraySum', () => {
  it('adds the array members', () => {
    assert.equal(arraySum([1,2,6]), 9);
  });
});
